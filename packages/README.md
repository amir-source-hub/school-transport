# Shared packages

This directory is reserved for the documented shared packages. A package should be added only when genuine reuse exists and its public API is approved; application-specific code remains inside its owning application or feature.

Planned package areas include UI, generated API client, shared types and validation, configuration, observability, and testing support.
