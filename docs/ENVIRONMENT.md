# Environment inventory

Real secrets belong in the deployment secret store, never in examples or source control. Build-time
variables require a rebuild; runtime variables require a process restart.

| Variable                                                                                               | Owner / phase           | Required             | Secret             | Validation / safe example                                                            |
| ------------------------------------------------------------------------------------------------------ | ----------------------- | -------------------- | ------------------ | ------------------------------------------------------------------------------------ |
| `NODE_ENV`                                                                                             | API/web runtime         | all                  | no                 | `development`, `test`, or `production`                                               |
| `PORT`, `HOST`                                                                                         | API runtime             | optional             | no                 | port number / bind address                                                           |
| `DATABASE_URL`                                                                                         | API, worker, migrations | all DB operations    | yes                | explicit PostgreSQL URL; no implicit fallback                                        |
| `TEST_DATABASE_URL`                                                                                    | integration tests       | integration CI       | yes                | dedicated disposable PostgreSQL database only                                        |
| `REDIS_URL`                                                                                            | API/worker runtime      | all queue operation  | yes                | explicit Redis URL; production requires password                                     |
| `PG_POOL_MAX`                                                                                          | API runtime             | optional             | no                 | integer 1–100                                                                        |
| `PG_IDLE_TIMEOUT_MS`                                                                                   | API runtime             | optional             | no                 | 1,000–300,000 ms                                                                     |
| `PG_CONNECT_TIMEOUT_MS`                                                                                | API runtime             | optional             | no                 | 250–30,000 ms                                                                        |
| `PG_STATEMENT_TIMEOUT_MS`                                                                              | API runtime             | optional             | no                 | 250–120,000 ms                                                                       |
| `PG_SSL_MODE`                                                                                          | API/worker runtime      | optional             | no                 | `disable` or `verify-full`                                                           |
| `READINESS_TIMEOUT_MS`                                                                                 | API/worker runtime      | optional             | no                 | 100–10,000 ms                                                                        |
| `JWT_SECRET`                                                                                           | API runtime             | all                  | yes                | 16+ chars; production requires 32+ non-placeholder chars                             |
| `JWT_ACCESS_TOKEN_TTL`, `JWT_REFRESH_TOKEN_TTL`, `JWT_REMEMBER_REFRESH_TOKEN_TTL`                      | API runtime             | optional             | no                 | positive seconds; refresh defaults 24h, remembered 7 days                            |
| `ADMIN_JWT_ACCESS_TOKEN_TTL`, `ADMIN_JWT_REFRESH_TOKEN_TTL`, `ADMIN_JWT_REMEMBER_REFRESH_TOKEN_TTL`    | API runtime             | optional             | no                 | positive seconds; refresh defaults 24h, remembered 7 days                            |
| `OTP_EXPIRY_SECONDS`, `OTP_MAX_ATTEMPTS`, `OTP_RESEND_COOLDOWN_SECONDS`, `ADMIN_CHALLENGE_TTL_SECONDS` | API runtime             | optional             | no                 | bounded positive integers; admin challenge default 5 minutes                         |
| `ONBOARDING_SESSION_TTL_SECONDS`                                                                       | API runtime             | optional             | no                 | positive seconds; onboarding session default 7 days                                  |
| `CORS_ORIGINS`                                                                                         | API runtime             | optional             | no                 | comma-separated exact origins                                                        |
| `TRUSTED_PROXY_CIDRS`                                                                                  | API runtime             | proxy deployments    | security-sensitive | comma-separated IP/CIDR; no catch-all                                                |
| `LOG_LEVEL`                                                                                            | API runtime             | optional             | no                 | production must not use `debug`                                                      |
| `METRICS_BEARER_TOKEN`                                                                                 | metrics scraper         | production           | yes                | 32+ random characters; sent only as the `/metrics` bearer token                      |
| `OTP_PROVIDER`, `SMS_PROVIDER`, `PAYMENT_GATEWAY_PROVIDER`                                             | API runtime             | all                  | no                 | use `kavenegar` for OTP/SMS; payments stay `none` until a real gateway is integrated |
| `KAVEHNEGAR_API_KEY`                                                                                   | API runtime             | Kavenegar enabled    | yes                | secret API key; never commit or log                                                  |
| `KAVEHNEGAR_BASE_URL`, `KAVEHNEGAR_TIMEOUT_MS`                                                         | API runtime             | Kavenegar enabled    | no                 | official HTTPS API root; timeout 500–30,000 ms                                       |
| `KAVEHNEGAR_SENDER`, `KAVEHNEGAR_OTP_TEMPLATE`                                                         | API runtime             | Kavenegar enabled    | partly             | optional ordinary-SMS sender; approved VerifyLookup template required for OTP        |
| `FEATURE_SMS_BROADCASTS`                                                                               | API/worker runtime      | optional             | no                 | disabled-by-default gate for admin SMS campaigns                                     |
| `SMS_BROADCAST_BATCH_SIZE`, `SMS_BROADCAST_MAX_RECIPIENTS`, `SMS_BROADCAST_MAX_SEGMENTS`               | API/worker runtime      | broadcasts enabled   | no                 | bounded throughput, audience, and message-size safeguards                            |
| `SMS_BROADCAST_PRICE_PER_SEGMENT_RIAL`, `SMS_BROADCAST_MAX_COST_RIAL`                                  | API runtime             | broadcasts enabled   | no                 | configurable estimate and hard spend cap from the provider contract                  |
| `SMS_BROADCAST_TEST_NUMBERS`                                                                           | API runtime             | broadcasts enabled   | privacy-sensitive  | comma-separated allowlist for test sends                                             |
| `SERVICE_ROLE`                                                                                         | API/worker runtime      | all                  | no                 | `api` or `worker`                                                                    |
| `AUTH_SESSION_RETENTION_DAYS`                                                                          | API runtime             | optional             | privacy-sensitive  | integer >= 1                                                                         |
| `QUEUE_REQUIRED`, `SEED_DEMO_DATA`                                                                     | API/bootstrap runtime   | optional             | no                 | `true`/`false`; production forbids demo seed                                         |
| `SEED_ADMIN_PASSWORD`                                                                                  | bootstrap runtime       | optional             | yes                | local demo-admin password; defaults to `demo-admin-password`; minimum 8 characters   |
| `NEXT_PUBLIC_API_BASE_URL`                                                                             | web build/browser       | production           | public             | absolute HTTPS URL (loopback allowed locally)                                        |
| `NEXT_PUBLIC_ASSET_BASE_URL`                                                                           | web build/browser       | production           | public             | immutable public asset release URL; absolute HTTPS (loopback allowed locally)        |
| `API_INTERNAL_BASE_URL`                                                                                | web server runtime      | production Compose   | no                 | internal absolute API URL                                                            |
| `NEXT_DEPLOYMENT_ID`                                                                                   | web build               | production           | no                 | immutable 7–128 character release ID                                                 |
| `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY`                                                                   | web build secret        | production           | yes                | base64 AES key, 16/24/32 decoded bytes                                               |
| `FEATURE_ADMIN_2FA`, `FEATURE_ONBOARDING`                                                              | API runtime             | optional             | no                 | boolean safe rollout gates; production target is `true`                              |
| `ARVAN_S3_ENDPOINT`, `ARVAN_S3_REGION`, `ARVAN_S3_BUCKET`                                              | API/worker runtime      | photo/receipt upload | security-sensitive | configure together for a private S3-compatible bucket                                |
| `ARVAN_S3_ACCESS_KEY`, `ARVAN_S3_SECRET_KEY`                                                           | API/worker runtime      | photo/receipt upload | yes                | scoped object-storage credentials; never expose to web/browser                       |
| `STUDENT_PHOTO_UPLOAD_URL_TTL_SECONDS`, `STUDENT_PHOTO_VIEW_URL_TTL_SECONDS`                           | API runtime             | optional             | no                 | short-lived signed URL TTLs; defaults to five minutes                                |
| `STUDENT_PHOTO_MAX_BYTES`, `STUDENT_PHOTO_MAX_PIXELS`, `STUDENT_PHOTO_MAX_AXIS`                        | API/worker runtime      | optional             | no                 | student photos are capped at 5 MiB; bounded image decode/upload resource limits      |
| `STUDENT_PHOTO_OUTPUT_WIDTH`, `STUDENT_PHOTO_OUTPUT_HEIGHT`, `STUDENT_PHOTO_JPEG_QUALITY`              | API/worker runtime      | optional             | no                 | canonical output is 600×800 JPEG at bounded quality                                  |
| `STUDENT_PHOTO_MAX_ACTIVE_UPLOADS`                                                                     | API runtime             | optional             | no                 | per-account active upload cap                                                        |
| `NEXT_PUBLIC_FEATURE_ADMIN_2FA`, `NEXT_PUBLIC_FEATURE_ONBOARDING`, `NEXT_PUBLIC_FEATURE_STUDENT_PANEL` | web build/browser       | optional             | public             | boolean rollout gates compiled into the immutable web release                        |
| `MAP_TILE_BASE_URL`                                                                                    | web server runtime      | optional             | no                 | approved HTTPS raster-tile origin; defaults to official OpenStreetMap                |
| `MAP_TILE_CONTACT_URL`                                                                                 | web server runtime      | production           | public             | public contact URL included in the stable tile-proxy User-Agent                      |
| `API_DOCS_ENABLED`                                                                                     | API runtime             | optional             | no                 | expose OpenAPI only in controlled environments; production default is `false`        |
| `E2E_WEB_PORT`, `E2E_API_PORT`, `E2E_BASE_URL`, `E2E_BROWSER_CHANNEL`, `E2E_REUSE_SERVER`, `E2E_VIDEO` | Playwright              | optional             | no                 | isolated test ports, URL, browser, reuse, and video controls                         |
| `CI`                                                                                                   | test runners            | CI only              | no                 | runner-provided truthy flag controlling retries, workers, and strictness             |
| `PERFORMANCE_BASE_URL`, `PERFORMANCE_BROWSER_CHANNEL`, `PERFORMANCE_BROWSER_EXECUTABLE_PATH`           | browser performance     | optional             | no                 | audited URL and installed Chrome/Chromium selection                                  |

Compose additionally consumes `POSTGRES_PASSWORD` and `REDIS_PASSWORD` from its secret source.
Changing any API variable requires restarting API and worker. Changing public web or deployment
variables requires rebuilding the single promoted web image. Database tooling fails before making
changes when `DATABASE_URL` is absent.

## Supported loading paths

- Full-stack Docker development uses the explicitly selected ignored root `.env` through
  `docker compose --env-file .env -f docker-compose.local.yml ...`. The local Compose file adapts
  database/Redis addresses to service DNS and uses a fixed public development-only Server Actions
  key; production does neither.
- Direct pnpm development uses `apps/api/.env` for the API and `apps/web/.env.local` for Next.js;
  workspace-root `.env` files are not implicitly loaded by those commands.
- Ignored root `.env*` files are local operator inputs only. Staging and production values must be
  injected by the hosting secret manager and never copied into images, CI artifacts, logs,
  screenshots, support bundles, or `NEXT_PUBLIC_*` variables.
- Kavenegar credentials are API-only. The inventory checker and Docker ignore policy prevent local
  environment files from entering build contexts; examples contain names and safe placeholders only.
